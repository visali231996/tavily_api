# json_extractor
this is used to extract the product information from the user input, does not give own answers..use one shot prompt method to display output 


github:
git clone url
git init
git status
git switch -c feature/jsonextractor
git add .
git commit -m "created json_extractor file"
